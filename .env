# Spotify API credentials — values are 1Password vault references.
# Real secrets are never stored here; they are injected at runtime by `op run`.
# See README.md for setup instructions.
SPOTIFY_CLIENT_ID=op://Personal/Spotify API/client_id
SPOTIFY_CLIENT_SECRET=op://Personal/Spotify API/client_secret
