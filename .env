# Spotify API credentials — values are Proton Pass vault references.
# Real secrets are never stored here; they are injected at runtime by `pass-cli run`.
# See README.md for setup instructions.
SPOTIFY_CLIENT_ID=pass://Personal/Spotify API/client_id
SPOTIFY_CLIENT_SECRET=pass://Personal/Spotify API/client_secret
